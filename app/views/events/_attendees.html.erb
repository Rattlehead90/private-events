<% unless @event.attendees.empty? %>
  <h2 class="font-bold">List of Attendees: </h2>
  <ul>
    <% @event.attendees.each do |attendee| %>
      <li><%= attendee.name %></li>
    <% end %>
  </ul>
<% end %>